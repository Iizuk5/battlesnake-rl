services:
  - type: web
    name: battlesnake-rl
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --chdir server --timeout 30 app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
